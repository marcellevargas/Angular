-   Todo arquivo que irá interagir com o html precisa
    ser declarado no index.html

-   Dentro do arquivo app.ts eu crio uma instancia da classe
    negociaçãoController para acessar o evento de adicionar

-   A ordem de importação do script faz diferença, como
    o app.js recebe o negociacaoController.js
    ele precisa ser declarado antes do app.js

-   Se eu não quiser que o typescript adote o tipo any
    como padrão, eu posso adicionar ao meu tsconfig.json
    o "noImplicitAny": true

-   Depois de declarar o tipo na classe, é nescessário
    ajustar o negociacaoController para aceitar a tipagem
    ca classe negociacao. Como os elementos recebidos do
    formulário são strings (usando <HTMLInputElement>) 
    eu preciso converter os atributos para o tipo correto
